.TH gfarm_stringlist_init 3 "1 May 2002"

.SH NAME

gfarm_stringlist_init \- stringlist を初期化する

.SH SYNOPSIS

.B "#include <gfarm/gfarm.h>"
.LP
.B char *
.br
.BI "gfarm_stringlist_init(gfarm_stringlist *" listp );

.SH DESCRIPTION

gfarm_stringlist は、文字列配列の動的確保を補助する型です。
動的に確保されるのは、配列だけであり、配列要素である各文字列については、
この型の管理外になります。

この関数は、
.I listp
の指す gfarm_stringlist 型の変数を初期化します。

この型の利用を終えた時には、gfarm_stringlist_free() を用いて、
動的に確保したメモリを解放する必要があります。

.SH "RETURN VALUES"

.TP
.SB NULL
.br
正常終了を示します。
.TP
.SB GFARM_ERR_NO_MEMORY
.br
メモリ不足のため処理を中止しました。
.TP
その他
.br
上記以外のエラーが生じたことを示します。

.SH "SEE ALSO"
.BR GFARM_STRINGLIST_ELEM (3),
.BR GFARM_STRINGLIST_STRARRAY (3) ,
.BR gfarm_stringlist_add (3),
.BR gfarm_stringlist_cat (3),
.BR gfarm_stringlist_elem (3),
.BR gfarm_stringlist_free (3),
.BR gfarm_stringlist_free_deeply (3),
.BR gfarm_stringlist_length (3)
